{
  "name": "mines-stars-bot",
  "version": "0.1.0",
  "private": true,
  "main": "src/server/run.js",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push --preview-feature",
    "start": "node src/server/run.js"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "express": "^4.18.2",
    "prisma": "^5.0.0",
    "telegraf": "^4.12.2"
  }
}
